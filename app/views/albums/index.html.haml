%h1 Albums
=link_to "Create an Album", new_album_path
#albums
  =render @albums

/insert light box here if user clicks on an album
-if current_user && current_user.admin?
  =link_to "Delete Album", album_url(@album), :method => :delete, :confirm => "Are you sure?"